Bump ``literalizer`` to 2026.9.4.
TypeScript now annotates a top-level homogeneous map rendered as an object literal with ``Record<string, V>``, so the value can be indexed with a ``string`` key under ``tsc --strict``.
